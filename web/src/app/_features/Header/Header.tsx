import { HeaderPresenter } from './HeaderPresenter'
import { UserInfo } from './UserInfo'

export const Header = () => {
  return (
    <HeaderPresenter>
      <UserInfo />
    </HeaderPresenter>
  )
}
